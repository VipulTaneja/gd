# Code Audit Backlog — UI/UX
_Generated: 2026-07-08 · Focus: Color palette, spacing/alignment, component consistency, bugs, performance, look & feel, animation opportunities across the resident and admin UI_
_Legend: **Impact** H/M/L · **Complexity** H/M/L · **Status** Discovered | In Progress | Done | Deferred_
_Continues numbering from [`260704-ui-ux-backlog.md`](../archive/260704-ui-ux-backlog.md) (AUD-001–020) and [`260708-ui-ux-backlog-archived-2026-07-08.md`](../archive/260708-ui-ux-backlog-archived-2026-07-08.md) (AUD-021–136, 114 done)_
_Validated in code: 2026-07-08_

---

## Progress summary

| Bucket | Count | IDs | Status |
|--------|-------|-----|--------|
| **Sprint 1 + completed Sprint 2** (archived) | 115 | AUD-021–136 (excl. 127) | [Done → archive](../archive/260708-ui-ux-backlog-archived-2026-07-08.md) |
| **Done** (pending next archive) | 16 | AUD-127, AUD-139, AUD-140, AUD-142–153 | Code verified 2026-07-08 |
| **In Progress** | 1 | AUD-141 | Partial — API routes migrated |
| **Deferred** | 1 | AUD-154 | Too big for one pass — 11 files, mechanical fix each |
| **Open** | 0 | — | — |
| **Total in file** | **17** | AUD-127, AUD-139–154 | |

---

## Items

| ID | Sprint | Area | Description | Complexity | Impact | Pointers to Solve | Status |
|----|--------|------|-------------|------------|--------|-------------------|--------|
| AUD-127 | Sprint 2 | Frontend | `staff/page.tsx` has no `Suspense`/skeleton fallback around `HelpTabContent`, unlike other data-heavy pages, so the page renders an empty shell before the client-side fetch resolves. | Medium | Medium | Wrap the list rendering in `<Suspense fallback={<ListSkeleton />}>` once staff data fetching moves server-side (see AUD-152). | Done — resolved as part of AUD-152: `staff/page.tsx` now wraps the new server-fetched `StaffHelpList` in `<Suspense fallback={<ListSkeleton />}>`. |
| AUD-139 | Sprint 2 | Backend | `api/contacts/route.ts`'s POST handler never validates `category`/`typeOfService` against a known set — any free-text string is accepted, so typos fragment categories (e.g. "Plumber" vs "plumber") into separate filter groups. | Medium | Medium | 1) Define a canonical category list (alongside `NON_REVIEWABLE_CATEGORY` in `contact-reviews.ts`). 2) Validate `category` against it in the POST handler, returning 400 on unknown values, and drive the create-contact form's dropdown from the same list. | Done — `api/contacts/route.ts:25-26` uses `isValidContactCategory()` from `contact-category-style.ts`. |
| AUD-140 | Sprint 2 | Backend | `api/contacts/[id]/reviews/route.ts`'s rate limiter is an in-memory `Map` inside the route module — it resets on restart and doesn't work across multiple serverless instances, so the "10 reviews/min" cap is not actually enforced globally. | Medium | Low | Move rate limiting to a shared store if it needs to hold under multi-instance deployment; otherwise document it as best-effort per-instance only. | Done — local route `Map` removed; uses shared `checkUserRateLimit()` (`contacts/[id]/reviews/route.ts:52`). Per-instance limit in `lib/rate-limit.ts` remains (infra follow-up if global enforcement needed). |
| AUD-141 | Sprint 2 | Frontend | An admin-role gate check (`!["SUPER_ADMIN","ADMIN"].includes(...)`) is duplicated inline in 4+ files instead of reusing the shared `isAdmin()` helper from `@/lib/rbac` that `staff/page.tsx` already uses correctly. | Medium | Low | Standardize on the shared `isAdmin()` helper everywhere an admin-role gate is needed, removing the duplicated local implementations. | In Progress — `requireAdminApi()` migrated on 8 admin API routes; ~17 inline `["SUPER_ADMIN","ADMIN"].includes(...)` remain in pages, server actions, and `api/search/route.ts`. Consider sync `isAdminRole(globalRole)` for call sites that already have role in hand. |
| AUD-142 | Sprint 2 | Frontend | `global-search-dialog.tsx`'s search API results are inconsistent in which fields are populated across categories (`user` duplicates tower info between `subtitle`/`meta`; `staff` never sets `meta` at all), producing ragged-looking result rows. | Medium | Low | Normalize which fields each `SearchResultType` populates (e.g. `meta` always for a short tag, `subtitle` always for descriptive text) across the ~14 category builders in `api/search/route.ts`. | Done — removed the redundant `meta` (tower block) from the `user` builder in `api/search/route.ts` since `subtitle` already shows the full unit number (which embeds the tower letter). Several other categories (`staff`, `post`, `forum_thread`, `community`) also omit `meta`, but that's an intentional "no natural short tag" pattern, not a bug — full-file field normalization not otherwise warranted. |
| AUD-143 | Sprint 2 | Frontend | `global-search-dialog.tsx` has no request cancellation/race-guard — if an earlier, slower query's response arrives after a later, faster one, the stale response can overwrite the fresher results. | Medium | Low | Add an `AbortController` per request (aborting on new keystroke) or a request-id ref, ignoring responses that don't match the latest issued request. | Done — `src/components/shared/global-search-dialog.tsx` uses a `latestRequestId` ref, ignoring stale responses at every checkpoint (401, error, success). |
| AUD-144 | Sprint 2 | Frontend | `rich-text-editor.tsx`'s character counter never visually warns when the value exceeds `maxLength`, and there's no client-side enforcement — users only discover the 10,000-char cap when the server rejects the save. | Medium | Medium | 1) Turn the counter rose/red when over the limit. 2) Optionally add a soft client-side guard (e.g. a CharacterCount extension) for immediate feedback. | Done — counter turns rose when `plainLength > maxLength` in `src/components/shared/rich-text-editor.tsx`. Skipped the optional `CharacterCount` extension (explicitly marked optional in the pointer). |
| AUD-145 | Sprint 2 | Frontend | `file-upload.tsx` silently truncates the selection to `maxFiles` via `.slice()` with no feedback when the user selects more than the limit. | Medium | Medium | Surface an inline message ("Only the first N photos were kept") when `files.length > maxFiles`. | Done — `src/components/shared/file-upload.tsx` shows "Only the first N photos were kept" when truncated. |
| AUD-146 | Sprint 2 | Frontend | `faq-manage-panel.tsx`'s `moveItem`/`moveSection` reorder actions call a full `router.refresh()` after every single up/down click with no optimistic update — rapid clicking produces a burst of refreshes and the button appears to do nothing until each completes. | Medium | Low | Apply the array swap optimistically to local state immediately, then reconcile with the server response in the background. | Done — `src/components/faq/faq-manage-panel.tsx` lifted `sections` into local state (synced from the `initialSections` prop via the render-time "adjusting state" pattern, not an effect, to avoid the `set-state-in-effect` lint error); `moveSection`/`moveItem` apply the swap immediately and only call `onRefresh()` to revert on API failure. Verified with `tsc`/`eslint` and a live `/faq/manage` request. |
| AUD-147 | Sprint 2 | Frontend | `faq-manage-panel.tsx`'s section/item delete uses native `window.confirm()`, jarring and unstyled compared to the app's design system. | Medium | Low | Replace with the app's existing dialog/alert-dialog primitive for an on-brand confirmation modal. | Done — `faq-manage-panel.tsx` uses `ConfirmDialog`; no `window.confirm` in `src/`. |
| AUD-148 | Sprint 2 | Frontend | The `/faq` page's redirect for logged-in users happens client-side after hydration (reading `window.location.hash` before calling `router.replace`) instead of server-side, causing a visible flash of the public FAQ shell before redirecting (and no redirect at all with JS disabled). | Medium | Medium | Perform the redirect server-side (`redirect("/faq/app")`) for the common no-hash case in `faq/page.tsx`, keeping the client component only for the hash-preservation edge case. | Done — went further than the pointer: replaced the client `FaqLoginRedirect` component entirely with a plain server-side `redirect("/faq/app")`. URL fragments are never sent to the server and are preserved automatically by the browser across same-origin redirects, so no client-side hash-reading was needed even for that "edge case" — deleted `faq-login-redirect.tsx` as dead code. Verified `/faq` renders correctly for guests via the running dev server. |
| AUD-149 | Sprint 2 | Frontend | `communities/[id]/page.tsx` renders 4 mutually-exclusive membership-status badges (join button, tower-only notice, pending, member) and `communities/page.tsx` renders its own "Member"/"View" badges — all via hardcoded, slightly different (padding/size) inline colors instead of a shared badge component, so the identical "Member" state looks different on the list vs. detail page. | Medium | Medium | Add "member"/"pending"/"admin-role"/"view" badge variants to `FriendlyBadge` (or a small shared `StatusPill`) and replace all the hardcoded spans on both pages with it. | Done — added `MEMBER`/`VIEW` to `FriendlyBadge`'s `semantic` variant (`PENDING`/`ADMIN_ROLE` already existed); both pages now use `FriendlyBadge`, so "Member" renders identically everywhere. Left the per-member-in-list admin-star chip in `communities/[id]/page.tsx` alone — it wraps an interactive `UserLink` plus a star icon, not a flat text label, so it doesn't fit `FriendlyBadge`'s value→label shape without a bigger restructure. |
| AUD-150 | Sprint 2 | Frontend | Neither `communities/page.tsx` nor `communities/[id]/page.tsx` has a `Suspense` boundary or loading skeleton, despite `src/components/shared/skeletons.tsx` existing for exactly this purpose — navigation blocks on all sequential DB calls with no visible loading state. | Medium | Medium | Split each page into a thin shell plus an inner async component wrapped in `<Suspense fallback={<...Skeleton />}>`, adding a skeleton variant if missing. | Done — added `CardGridSkeleton({cards, cols})` to `skeletons.tsx`; `communities/page.tsx` split into thin shell (auth + `PageHeader`) + inner `CommunitiesGrid` async component; `communities/[id]/page.tsx` split into thin shell (auth + `params` + user lookup for `DashboardLayout`) + inner `CommunityDetail` async component (community fetch incl. `notFound()`, membership/leader logic, all section rendering), both wrapped in `<Suspense fallback={<CardGridSkeleton .../>}>`. Verified `tsc --noEmit`, `eslint`, and live requests to `/communities` and `/communities/nonexistent-id` (unauth redirect to `/login` unaffected, no server/client leak regression). |
| AUD-151 | Sprint 2 | Frontend | `casual-header.tsx`'s search trigger reuses the same modal-sized `GlobalSearchDialog` panel at every breakpoint instead of a full-screen/bottom-sheet layout on mobile, unlike the abandoned `MobileSearch` component which had a proper touch-friendly layout. | Medium | Low | On small screens, render the dialog panel full-width/full-height (`inset-0`, no `max-w-lg` cap) instead of a shrunk desktop-style modal. | Done — `global-search-dialog.tsx`'s panel is now `h-full w-full` (no `max-w-lg`/rounding/border) below `sm:`, reverting to the original centered `max-w-lg` modal at `sm:` and up; results list uses `flex-1` on mobile so it fills the viewport instead of a fixed `max-h-80`. Added a 44×44px close (`X`) button, visible only on mobile (`sm:hidden`), since the full-screen panel leaves no backdrop to tap. Verified `tsc`/`eslint` clean and no regressions via the running dev server. |
| AUD-152 | Sprint 3 | Frontend | `HelpTabContent` is a full client component that fetches all staff data client-side via `useEffect` + `fetch("/api/staff")` after mount, showing a loading spinner on every page load, while `contacts/page.tsx` fetches server-side and passes props with no spinner or waterfall — the single biggest perf/consistency gap between these two sibling list pages. | High | High | 1) Move the staff-list DB query into `src/app/staff/page.tsx` as a server-side fetch, mirroring `contacts/page.tsx`'s `db.importantContact.findMany` call. 2) Pass the resulting list as a prop into `HelpTabContent`, keeping it client-side only for interactive filtering/add/remove. 3) Keep a `router.refresh()`-based reload path for mutations instead of the current fetch-on-mount pattern. | Done — extracted the GET handler's logic from `api/staff/route.ts` into a shared `getStaffListForCaller(userId)` in `lib/staff.ts` (also used by the route itself, removing duplication). `staff/page.tsx` now calls it server-side inside a new `StaffHelpList` async component and passes `staff` as a prop into `HelpTabContent`, which dropped its `useState`/`useEffect` fetch-on-mount entirely (`staff` is now a plain prop). `confirmEndAssociation`/`submitAddToUnit`/`StaffAssociateForm.onSuccess` now call `router.refresh()` instead of a client re-fetch. Also resolved AUD-127 as a byproduct (same `StaffHelpList` extraction naturally took the `<Suspense>` wrap). Verified `tsc`/`eslint` clean and `/staff` returns 200 with no compile errors. |
| AUD-153 | Sprint 3 | Frontend | `units/[unitNumber]/page.tsx` and `users/[userId]/page.tsx` use raw shadcn `Card`/`CardHeader`/`Badge` enterprise-style markup instead of the `SoftCard`/`FriendlyBadge`/`PageHeader` conventions used throughout the rest of the audited resident UI (contacts, staff). | High | Medium | 1) Replace the raw `<h1>` headers with `<PageHeader feature="directory" .../>` on both pages. 2) Swap `Card`/`CardContent` wrappers for `SoftCard`, and raw `<Badge>{role.replace(...)}</Badge>` for `FriendlyBadge` wherever role/status enums are rendered. 3) Consider scoping the badges first (lower risk) before migrating card chrome. | Done — both pages now use `<PageHeader feature="directory" .../>` and `SoftCard` (with a plain `<h2>` section title above each card, matching the pattern already used in `staff/[id]/page.tsx`/`contacts/[id]/page.tsx`) in place of every `Card`/`CardHeader`/`CardTitle`/`CardContent`. Added `OWNER`/`JOINT_OWNER`/`TENANT`/`OWNER_FAMILY`/`TENANT_FAMILY`/`PRIMARY` to `FriendlyBadge`'s `semanticConfig` (`UnitRole` had no prior label mapping) and replaced every raw `<Badge>{role.replace(...)}</Badge>`/`{cm.role}`/`Active` span with `FriendlyBadge` — `CommunityRole`'s `ADMIN`/`MEMBER` and the visitor-pass `Active` status already matched existing entries. Verified `tsc`/`eslint` clean and both routes compile with no runtime errors. |
| AUD-154 | Sprint 3 | Frontend | Several admin pages (`committee`, `communities`, `dues`, `export`, `audit`, `notices/new`, `forums`, `tickets`) wrap their content in `DashboardLayout` (the resident casual shell) even though `admin/layout.tsx` already wraps every `/admin/*` route in its own dark-sidebar enterprise chrome — producing a double-shell / nested-navigation bug on those pages. | High | High | 1) Load `/admin/committee` and confirm whether the casual header/bottom-nav renders nested inside the dark admin sidebar. 2) If confirmed, remove the `DashboardLayout` wrapper from all affected admin pages. 3) Standardize every admin page on the `Breadcrumb`-only convention already used by `facilities`, `vehicles`, `moves`, `staff`, and `units`. | ⏳ Deferred — Too Big. Confirmed via `admin/layout.tsx` (already does its own session/role redirect and provides the dark sidebar chrome) and `admin/committee/page.tsx` (fetches `user` solely to pass into `DashboardLayout`) that the bug is real: 11 files still wrap in `DashboardLayout` — `admin/{committee,dues,export,audit,forums,tickets,notices/new,units,units/[id],communities,communities/[id]}/page.tsx`. The fix itself is mechanical and uniform per file (drop the `DashboardLayout` import/wrapper and the layout-only `db.user.findUnique` fetch, add a `<Breadcrumb>` matching the `admin/moves/page.tsx` reference pattern), but touches 11 independent files — over the single-item size threshold. Left untouched pending a dedicated pass. |

---

## Notes for the executor

- **AUD-154** (admin double-shell, 11 files) is deferred as too big for a single pass — the fix is mechanical and uniform (see its Status cell for the exact per-file recipe and file list), a good candidate for a dedicated follow-up.
- **AUD-141** remains in progress (~17 inline `isAdmin` gate checks left to migrate).
- **AUD-139, AUD-140, AUD-142–153** verified done in code — archive on next `summarize and clean`.
- All other items in this file are resolved or deferred — no `Discovered` items remain.
- **Continued in [`260708-refactor-backlog.md`](./260708-refactor-backlog.md)** (AUD-155–214): Sprint 4–5 (30 active items).
