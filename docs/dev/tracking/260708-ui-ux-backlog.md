# Code Audit Backlog — UI/UX
_Generated: 2026-07-08 · Focus: Color palette, spacing/alignment, component consistency, bugs, performance, look & feel, animation opportunities across the resident and admin UI_
_Legend: **Impact** H/M/L · **Complexity** H/M/L_
_Continues numbering from the archived [`260704-ui-ux-backlog.md`](../archive/260704-ui-ux-backlog.md) (AUD-001–020, all Done)._

---

## Items

| ID | Sprint | Area | Description | Complexity | Impact | Pointers to Solve | Status |
|----|--------|------|-------------|------------|--------|-------------------|--------|
| AUD-021 | Sprint 1 | Backend | `src/app/api/contacts/[id]/reviews/route.ts` PUT handler calls `await request.json()` twice (once for `isHidden`, again for `reviewId`) — a Request body stream can only be read once, so this throws at runtime and every hide/unhide review action fails. | Low | High | Merge into a single destructure: `const { isHidden, reviewId } = await request.json();` and remove the duplicate call. | Discovered |
| AUD-022 | Sprint 1 | Frontend | `src/app/admin/communities/page.tsx`'s `JoinRequestRow` calls `React.useState` and misuses `startTransition` as a boolean setter (`startTransition(true)`) inside a Server Component (no `"use client"`) — this throws "Invalid hook call" and breaks the entire pending join-request approve/reject UI. | Medium | High | 1) Extract `JoinRequestRow` into its own file with `"use client"`. 2) Use `const [pending, startTransition] = useTransition()` correctly, calling `startTransition(() => handleJoinRequest(...))`. 3) Import `handleJoinRequest` statically. | Discovered |
| AUD-023 | Sprint 1 | Frontend | `src/components/shared/file-upload.tsx`'s `removeFile` recomputes the filtered preview list correctly but then calls `onFilesSelected([])` unconditionally — removing one of several selected photos silently clears the entire selection the parent form thinks it has. | Low | High | Track the underlying `File[]` in state, and call `onFilesSelected(files.filter((_, idx) => idx !== i))` instead of `onFilesSelected([])`. | Discovered |
| AUD-024 | Sprint 1 | Frontend | `src/app/facilities/[id]/booking-grid.tsx` destructures `isPending` away from `useTransition()` (`const [, startTransition] = useTransition()`), so no slot is disabled while a booking request is in flight — a user can tap multiple slots (or the same one twice, re-triggering `confirm()`) before the first request resolves. | Low | High | 1) Capture `isPending`. 2) Disable all slot buttons/cells while `isPending`. 3) Track a `bookingSlotKey` state for a per-cell loading indicator. | Discovered |
| AUD-025 | Sprint 1 | Frontend | `src/lib/microcopy.ts`'s `faq.title` is the literal string `"Help &amp; FAQ"`, but every consumer renders it as plain JSX text (`{faqCopy.title}` in `faq/page.tsx`, `PageHeader` title prop, and page `<title>`/OG tags) — React doesn't decode HTML entities in text nodes, so it literally displays `Help &amp; FAQ`. | Low | High | Change the string to a plain ampersand: `title: "Help & FAQ"`. | Discovered |
| AUD-026 | Sprint 1 | Frontend | `src/app/staff/[id]/page.tsx:64` passes `feature="visitors"` to `PageHeader` instead of `feature="staff"` — a copy-paste mistake that shows the wrong icon/color (orange `DoorOpen`) on the staff profile detail page. | Low | Medium | Change `feature="visitors"` to `feature="staff"`. | Discovered |
| AUD-027 | Sprint 1 | Frontend | `src/lib/feature-colors.ts` has 5 duplicate color pairs across otherwise-distinct features: `notices`/`communities` (amber), `tickets`/`team` (rose), `facilities`/`contacts` (emerald), `visitors`/`staff` (orange), `notifications`/`faq` (indigo) — undermining the "one pastel per feature" convention. | Low | Medium | Reassign one color in each pair to an unused pastel (e.g. violet/cyan/lime are free) so every `FeatureKey` has a unique bg/text/ring combination. | Discovered |
| AUD-028 | Sprint 1 | Frontend | `src/components/shared/status-badge.tsx` (`StatusBadge`) duplicates `FriendlyBadge` with different, inconsistent colors for the same status values (e.g. `OPEN` is `blue-100` here vs `sky-100` in `FriendlyBadge`) and is still imported in `src/app/tickets/page.tsx`. | Low | Medium | Replace the `StatusBadge` import/usage in `tickets/page.tsx` with `FriendlyBadge`, then delete `status-badge.tsx`. | Discovered |
| AUD-029 | Sprint 1 | Frontend | `src/app/admin/users/page.tsx` defines its own local `StatusBadge` function (lines 56-67) — a *third* independent hardcoded status-color implementation, separate from both `shared/status-badge.tsx` and `FriendlyBadge`. | Low | Medium | Replace the local `StatusBadge` function with `FriendlyBadge` (extending its status map with `PENDING`/`APPROVED`/`REJECTED` if missing). | Discovered |
| AUD-030 | Sprint 1 | Frontend | `HubCommunityPulse` (rendered from `src/app/page.tsx:43`) renders fully hardcoded stats ("204 homes", "3 towers", "IGBC Platinum", "5.8 acres") as a static array, not sourced from the DB via `hub-data.ts`. | Low | Medium | Wire real unit/tower counts from `hub-data.ts` into the component props, or clearly document the stats as intentionally static marketing copy. | Discovered |
| AUD-031 | Sprint 1 | Frontend | `src/components/hub/hub-hero.tsx`'s `statChips` array duplicates the exact same hardcoded stats already hardcoded separately in `hub-community-pulse.tsx` — two independent copies that can drift out of sync. | Low | Low | Extract a single shared `communityStats` constant and import it in both files. | Discovered |
| AUD-032 | Sprint 1 | Frontend | `src/components/hub/hub-greeting.tsx` is dead code — never imported anywhere; `page.tsx` uses `HubHero` for the greeting instead, which duplicates the same `getGreeting()`/name/`UnitLink` logic. | Low | Low | Delete `hub-greeting.tsx`, or document why it's intentionally kept. | Discovered |
| AUD-033 | Sprint 1 | Frontend | `getGreeting()` (time-of-day greeting) is duplicated verbatim between `hub-hero.tsx` and `hub-greeting.tsx`. | Low | Low | Move `getGreeting()` into `src/lib/microcopy.ts` (or a new `src/lib/greeting.ts`) and import from both call sites. | Discovered |
| AUD-034 | Sprint 1 | Frontend | `src/components/shell/horizontal-nav-pills.tsx` (`HorizontalNavPills`) appears unused outside its own file and duplicates `casual-header.tsx`'s active-pill styling logic almost identically. | Low | Low | Confirm via repo-wide usage search; if unused, delete the file. | Discovered |
| AUD-035 | Sprint 1 | Frontend | `src/components/hub/community-hub.tsx:50` renders `<MobileBottomNav isAuthenticated={isAuthenticated} />` without passing `isLeader`, so a signed-in leader on the Hub ("/") never sees the "Leader hub" entry in the bottom-nav "More" sheet (it only appears via `ResidentShell` on other pages). | Low | Medium | Derive `isLeader` in `page.tsx` (same pattern as `DashboardLayout`) and forward it through `CommunityHub` to `MobileBottomNav`. | Discovered |
| AUD-036 | Sprint 1 | Frontend | `src/components/shell/mobile-bottom-nav.tsx`'s FAQ entry in `moreLinks` compares active state against a conditional `linkHref` (`/faq/app` vs `/faq`), so a user browsing `/faq` directly never sees the FAQ tile marked active. | Low | Low | Special-case `feature === "faq"` to match active state on any `/faq` prefix, mirroring how `"staff"` is already special-cased. | Discovered |
| AUD-037 | Sprint 1 | Frontend | `mobile-bottom-nav.tsx` hardcodes a check that returns `null` for any path starting with `/admin`, even though `admin/layout.tsx` already renders its own separate shell and never includes `MobileBottomNav`. | Low | Low | Confirm no code path renders `ResidentShell` under `/admin`, then remove the redundant pathname check. | Discovered |
| AUD-038 | Sprint 1 | Frontend | `src/components/hub/hub-amenity-chips.tsx` receives `facility.location` from the DB (via `hub-data.ts`) but never displays it — fetched and typed but silently dropped in the UI. | Low | Low | Either surface `location` as secondary text on the amenity chip, or drop the field from the Prisma `select`. | Discovered |
| AUD-039 | Sprint 1 | Frontend | `src/components/hub/hub-live-feed.tsx:139` — `{"title" in item.data ? item.data.title : ""}` is dead code: every variant of the `FeedItem` union declares `title: string`, so the check is always true and the else branch is unreachable. | Low | Low | Simplify to `{item.data.title}` directly. | Discovered |
| AUD-040 | Sprint 1 | Frontend | `hub-live-feed.tsx`'s `typeConfig` hardcodes its own bg/text pastel classes per feed-item type instead of reusing `featureColors`, risking silent drift if pastels are retuned later. | Low | Low | Replace `typeConfig`'s bg/text/icon with lookups into `featureColors[feature]`, keeping only the "poll" special case if no equivalent entry exists. | Discovered |
| AUD-041 | Sprint 1 | Frontend | `hub-live-feed.tsx` mixes border-radius scales on the same row: the outer `Link` uses `rounded-xl` while the icon avatar wrapper uses `rounded-lg`. | Low | Low | Align the icon avatar wrapper to `rounded-xl` to match the card radius family. | Discovered |
| AUD-042 | Sprint 1 | Frontend | `casual-header.tsx`'s fallback `useEffect` polls `/api/notifications/unread` client-side when `unreadProp` is undefined, but every current call site always passes a concrete `unreadCount` — this path is dead/unreachable overhead. | Low | Low | Confirm no call site omits `unreadCount`; if none do, remove the fallback fetch effect. | Discovered |
| AUD-043 | Sprint 1 | Frontend | `login/page.tsx`'s dev quick-login buttons use `rounded-md` while every other button/input on the page uses `rounded-lg`. | Low | Low | Change the dev quick-login button className to `rounded-lg`. | Discovered |
| AUD-044 | Sprint 1 | Frontend | `src/app/leader/page.tsx`'s `leader-hub.tsx` `PageHeader` uses `feature="directory"` for the unrelated "Leader hub" page, borrowing the teal directory color/icon. | Low | Low | Add a dedicated `"leader"` `FeatureKey` (reusing the `Crown` icon already used for it in `mobile-bottom-nav.tsx`) and use that instead. | Discovered |
| AUD-045 | Sprint 1 | Frontend | `src/app/admin/layout.tsx:31` casts `(session?.user as { globalRole?: string })?.globalRole` with an ad hoc inline type cast instead of a properly typed session augmentation. | Low | Low | Check whether `globalRole` is declared on the session user type; if not, add it there so callers avoid the cast. | Discovered |
| AUD-046 | Sprint 1 | Frontend | `admin/layout.tsx`'s unauthorized-access redirect target is `/dashboard`, which does not appear to be a real route (the resident home is `/`; `DashboardLayout` is only a wrapper component) — rejected admin visitors likely hit a 404. | Low | Medium | Verify `src/app/dashboard/page.tsx` doesn't exist; if so, change the redirect target to `/`. | Discovered |
| AUD-047 | Sprint 1 | Frontend | `admin/layout.tsx`'s desktop sidebar nav links have no active-state styling at all, while `admin-mobile-nav.tsx` correctly computes and highlights the active route — desktop admin users get no indication of which section they're in. | Medium | High | 1) `admin/layout.tsx` is a Server Component, so it can't call `usePathname()` directly — extract the nav list into a `"use client"` `AdminSidebarNav` component. 2) Compute active state via `usePathname()` mirroring `admin-mobile-nav.tsx`. 3) Apply the same active/inactive classes used there. | Discovered |
| AUD-048 | Sprint 1 | Frontend | `src/components/hub/contact-rwa-dialog.tsx` never checks `res.ok` before calling `setSubmitted(true)` — a 4xx/5xx server error still shows "Message sent successfully!". | Low | Medium | Check `if (!res.ok) throw new Error(...)` before setting the submitted state. | Discovered |
| AUD-049 | Sprint 1 | Frontend | `contact-rwa-dialog.tsx`'s catch block only does `console.error` with no user-facing error state — a failed enquiry leaves the dialog with the button re-enabled and zero feedback. | Low | Medium | Add an error state set in the catch block and render it above the submit button, mirroring `login/page.tsx`'s `devError` pattern. | Discovered |
| AUD-050 | Sprint 1 | Frontend | `src/components/shared/icon-tile.tsx`'s count badge and `casual-header.tsx`'s notification badge each reimplement the identical "99+" bubble independently with slightly different sizing (`h-5 min-w-5` vs `h-4 min-w-4`). | Low | Low | Extract a shared `CountBadge` component (props: `count`, `size`) used by both. | Discovered |
| AUD-051 | Sprint 1 | Frontend | `src/lib/feature-colors.ts`'s `team` label ("Meet Our Team") breaks the terse 1-3 word convention every other label follows ("What's new", "Get help", "Neighbours"). | Low | Low | Shorten to "Our Team" or "Team". | Discovered |
| AUD-052 | Sprint 1 | Frontend | `src/components/contacts/contacts-list.tsx` destructures only `{ contacts }` from its props, silently dropping `currentUserId`/`isAdmin` passed by `contacts/page.tsx` — the "Add Contact" affordance shows to every resident with no admin gating despite the prop existing for that purpose. | Low | Medium | Wire `isAdmin`/`currentUserId` into the component to gate edit-sensitive UI, or remove the unused props to stop implying access control that doesn't exist. | Discovered |
| AUD-053 | Sprint 1 | Frontend | `window.location.reload()` is used after successful mutations in at least 6 places (`contact-review-form.tsx`, `contact-edit-form.tsx`, `staff-associate-form.tsx`, `admin-staff-review-actions.tsx`, `faq-manage-panel.tsx`, `booking-grid.tsx`) instead of `router.refresh()` — causing a visible flash and loss of scroll position/client state on an otherwise smooth-transition UI. | Low | Medium | Replace each `window.location.reload()` with `useRouter().refresh()` from `next/navigation`. | Discovered |
| AUD-054 | Sprint 1 | Frontend | `contacts/[id]/page.tsx:138-143` hardcodes cross-link copy inline instead of sourcing it from `src/lib/microcopy.ts` like the rest of the page. | Low | Low | Add a `crossLinkToStaff` entry to `contactsCopy` in microcopy.ts and reference it here. | Discovered |
| AUD-055 | Sprint 1 | Frontend | `microcopy.ts` has dead copy entries (`contactsCopy.detailTitle`, `staffCopy.viewProfile`) never referenced anywhere in the contacts/staff pages or components. | Low | Low | Wire `detailTitle` into the contact detail `PageHeader` subtitle and `viewProfile` into `StaffLink`/staff cards, or remove both unused entries. | Discovered |
| AUD-056 | Sprint 1 | Frontend | `contacts/[id]/page.tsx` and `staff/[id]/page.tsx` card grids use different column/gap schemes (`grid-cols-1..3` gap-2 vs `grid-cols-1..4` gap-3) despite being explicitly sibling registries meant to feel alike. | Low | Medium | Align both grids to the same column/gap scheme (recommend the staff version's denser layout). | Discovered |
| AUD-057 | Sprint 1 | Frontend | `contacts-list.tsx`'s empty state is a hand-rolled `<div>` instead of the shared `EmptyState` component, violating the project's own list-page convention. | Low | Medium | Replace the inline empty-state div with `<EmptyState icon={Users} title={...} description={...} />`, adding matching copy keys to `contactsCopy`. | Discovered |
| AUD-058 | Sprint 1 | Frontend | `contacts-list.tsx` recomputes `filteredContacts`/`groupedContacts` via plain `.filter()`/`.reduce()` on every render with no `useMemo`, unlike `help-tab-content.tsx`'s equivalent derived state which is memoized. | Low | Low | Wrap `categories`, `filteredContacts`, and `groupedContacts` in `useMemo` keyed on `[contacts, searchQuery, selectedCategory]`. | Discovered |
| AUD-059 | Sprint 1 | Frontend | None of the contacts, staff, or directory-tower list/grid renderings use `FadeIn`/`StaggerChildren` from `src/components/shared/animated.tsx`, despite being the project's designated entrance-animation primitives for exactly this content type. | Low | Low | Wrap the `.map()` output of each grid in `StaggerChildren`, respecting `prefers-reduced-motion` as the primitive already does. | Discovered |
| AUD-060 | Sprint 1 | Frontend | `units/[unitNumber]/page.tsx` renders open-ticket status as raw `<Badge variant="outline">{t.status}</Badge>` (e.g. `IN_PROGRESS`), directly violating the documented convention of using `FriendlyBadge` instead of raw enum labels. | Low | Medium | Replace with `<FriendlyBadge value={t.status} variant="status" />`. | Discovered |
| AUD-061 | Sprint 1 | Frontend | `users/[userId]/page.tsx`'s `roleBadgeColor` is a hand-rolled hardcoded color map applied to a raw `<Badge>`, and `user.globalRole.replace("_", " ")` only replaces the first underscore (fragile for any future 2+-underscore role). | Low | Low | Extract role-to-color mapping into a shared helper (or reuse `FriendlyBadge` if extended to support global roles); use `.replaceAll("_", " ")`. | Discovered |
| AUD-062 | Sprint 1 | Frontend | `contacts/[id]/page.tsx` and `staff/[id]/page.tsx` both duplicate the same inline "rating summary" JSX (star icon + numeric average + pluralized count) instead of reusing `StarRatingDisplay`. | Low | Low | Replace both inline blocks with `<StarRatingDisplay rating={...} reviewCount={...} showValue />` once extended (see next item). | Discovered |
| AUD-063 | Sprint 1 | Frontend | `StarRatingDisplay` silently drops the numeric average rating text that both `contacts/[id]` and `staff/[id]` need, which likely explains why those pages duplicate the summary JSX inline instead of reusing it. | Low | Low | Add an optional `showValue?: boolean` prop rendering the numeric `rating` (1 decimal) alongside the stars. | Discovered |
| AUD-064 | Sprint 1 | Frontend | `units/[unitNumber]/page.tsx` renders `"{pendingDues.length} pending due(s)"` with a literal `"(s)"` suffix instead of real pluralization, inconsistent with the correct pattern used on the review pages. | Low | Low | Replace with `` `${pendingDues.length} pending due${pendingDues.length === 1 ? "" : "s"}` ``. | Discovered |
| AUD-065 | Sprint 1 | Frontend | `src/components/units/unit-household-staff.tsx` returns `null` when a unit has no household staff — the section disappears entirely with no path to `/staff` to add one. | Low | Low | Render a compact empty-state row ("No regular help yet" + a "Manage" link) instead of `null`. | Discovered |
| AUD-066 | Sprint 1 | Frontend | `visitors/[id]/page.tsx` hardcodes its own `statusStyles` color map and renders the raw enum string for pass status, showing different colors/labels than `FriendlyBadge` (already used on `visitors/page.tsx`) for the identical status. | Low | Medium | Replace the inline map/span with `<FriendlyBadge value={pass.status} variant="status" />`. | Discovered |
| AUD-067 | Sprint 1 | Frontend | `gate-validation.tsx`'s OTP input has no `<form>`/`onKeyDown` handler, so pressing Enter does nothing — the guard must tap the button on every validation. | Low | Medium | Wrap input+button in a `<form onSubmit={...}>` so Enter submits. | Discovered |
| AUD-068 | Sprint 1 | Frontend | `gate-validation.tsx`'s result panel uses raw emoji and ad hoc green/red backgrounds with no entrance animation, despite `FadeIn`/`SuccessAnimation` being idiomatic elsewhere in the app for confirmation feedback. | Low | Medium | Wrap the valid-result block in `FadeIn`, and trigger `SuccessAnimation` on a successful validation. | Discovered |
| AUD-069 | Sprint 1 | Frontend | `gate-validation.tsx` gives no retry affordance on network failure beyond a generic "Network error" string, forcing the guard to retype the OTP from scratch on a potentially flaky gate connection. | Low | Medium | On catch, keep the entered OTP and surface a "Retry" button that re-calls `handleValidate()`. | Discovered |
| AUD-070 | Sprint 1 | Backend | `visitors/validate/route.ts` returns indistinguishable error text ("Invalid or expired pass" vs "Pass is not valid at this time") for two operationally different conditions (wrong/reused OTP vs correct OTP outside its valid window). | Low | Medium | Return a distinguishing `reason` code and show different guidance text per case in `gate-validation.tsx` (e.g. show the valid window when outside it). | Discovered |
| AUD-071 | Sprint 1 | Backend | `gate/package-received/route.ts` returns generic error messages ("Invalid or inactive pass") with no indication of the actual pass status (already used vs cancelled vs expired). | Low | Low | Return the actual `pass.status` in the error payload so any consuming UI can show a precise reason. | Discovered |
| AUD-072 | Sprint 1 | Frontend | The "Recurring" pass indicator is rendered two different ways: a hardcoded, non-token purple pill in `visitors/page.tsx` and plain purple text (no pill) in `gate-validation.tsx` — the same semantic state styled inconsistently in two places. | Low | Low | Extract a shared `RecurringBadge` (or reuse `FriendlyBadge`-style markup) used in both locations, sourcing its color from a token instead of inline purple. | Discovered |
| AUD-073 | Sprint 1 | Frontend | `booking-grid.tsx` calls `window.location.reload()` after a successful booking with no success feedback shown first — a jarring full-page flash instead of confirmation. | Low | Medium | Replace with `router.refresh()` and show `<SuccessAnimation message="Booked!" />` before refreshing. | Discovered |
| AUD-074 | Sprint 1 | Frontend | `booking-grid.tsx`'s `now` (`new Date()`) is captured fresh on every render with no periodic tick, so "past" slot styling can go stale if the grid stays open for a while with no other re-render trigger. | Low | Low | Either accept the staleness (likely fine for a slot grid) or add a `setInterval`-driven tick if live "past" updates matter. | Discovered |
| AUD-075 | Sprint 1 | Frontend | `booking-grid.tsx`'s desktop grid cells are `min-h-[36px]` — clickable (`role="button"`, `tabIndex=0`) but below the 44×44px touch-target minimum mandated project-wide, unlike the mobile buttons which correctly use `min-h-11`. | Low | Medium | Increase desktop cell `min-height` to `min-h-11` (44px). | Discovered |
| AUD-076 | Sprint 1 | Frontend | `booking-grid.tsx`'s desktop cells show an unexplained "Mnt" abbreviation for maintenance and convey availability by background color alone with no icon/text, failing for colorblind users. | Low | Low | Spell out "Maintenance" (or add a `title` attribute), and add a small icon on available cells so status isn't color-only. | Discovered |
| AUD-077 | Sprint 1 | Frontend | `booking-grid.tsx`'s error banner has no dismiss button and no auto-clear — a stale error can persist visually if a subsequent action doesn't overwrite it. | Low | Low | Add a dismiss (X) button and/or auto-clear the error after a timeout. | Discovered |
| AUD-078 | Sprint 1 | Frontend | `visitors/[id]/page.tsx`'s OTP text uses `break-all` (unnecessary for a fixed 6-digit number) and has no copy-to-clipboard button despite being the primary code a resident needs to relay. | Low | Low | Remove `break-all` and add a copy-to-clipboard icon button next to the OTP. | Discovered |
| AUD-079 | Sprint 1 | Frontend | `visitors/[id]/page.tsx`'s QR/OTP card has no entrance animation despite `FadeIn` being the app's standard pattern for content reveal elsewhere. | Low | Low | Wrap the QR/OTP card contents in `<FadeIn>`. | Discovered |
| AUD-080 | Sprint 1 | Backend | `visitors/route.ts`'s rate-limit error ("Maximum 10 active passes allowed") gives no actionable next step — no link to `/visitors` to review/cancel an existing pass. | Low | Low | Include `activeCount` in the response payload so the form UI can link to `/visitors` with guidance to cancel one. | Discovered |
| AUD-081 | Sprint 1 | Frontend | `admin/dues/page.tsx`'s `MarkPaidButton` has no `"use client"`/`useTransition` pending state, unlike every other admin mutation button — a double-click can fire duplicate mark-paid requests. | Low | Medium | Convert to a client component using `useTransition` with a disabled/`Loader2` state, mirroring `ApproveMoveButton` in `admin/moves/buttons.tsx`. | Discovered |
| AUD-082 | Sprint 1 | Frontend | `admin/staff/page.tsx` uses a bare "⚠" emoji with no `aria-label` as its "needs review" indicator, inconsistent with the colored pill badges used for equivalent states elsewhere in admin (e.g. `facilities/page.tsx`'s "Approval required" badge). | Low | Low | Replace the emoji with a small amber pill badge matching `facilities/page.tsx`'s pattern. | Discovered |
| AUD-083 | Sprint 1 | Frontend | `admin/staff/[id]/page.tsx` has no `Breadcrumb`/back-link to `/admin/staff`, unlike sibling admin detail pages (`facilities/[id]`, `units/[id]`, `communities/[id]`). | Low | Medium | Add `<Breadcrumb items={[{label:"Admin",href:"/admin"},{label:"Staff registry",href:"/admin/staff"},{label:person.name}]} />`. | Discovered |
| AUD-084 | Sprint 1 | Frontend | `admin/staff/page.tsx` has neither `DashboardLayout` nor a `Breadcrumb` — a bare `<div>` with an `<h1>` — leaving it visually orphaned relative to every other admin section. | Low | Medium | Add `<Breadcrumb items={[{label:"Admin",href:"/admin"},{label:"Staff registry"}]} />`, consistent with `facilities/page.tsx`. | Discovered |
| AUD-085 | Sprint 1 | Backend | `admin-staff-review-actions.tsx`'s PATCH fetch has no error handling — on failure it still calls `window.location.reload()` unconditionally, silently discarding the error with no admin feedback. | Low | Medium | Check `if (!res.ok)` and show an inline error before reloading, mirroring the `res.ok` check in `admin/users/actions.tsx`. | Discovered |
| AUD-086 | Sprint 1 | Frontend | `src/lib/admin-nav.ts` has no entries for Committee, Communities, Dues, Facilities, Forums, Moves, Tickets, Vehicles, Export, or Audit — over half of the real admin routes are unreachable from the sidebar/mobile nav without knowing the URL. | Medium | High | 1) Add nav entries for every orphaned route with an appropriate icon key. 2) Extend the `AdminNavIcon` union and `adminNavIcons` record in both `admin/layout.tsx` and `admin-mobile-nav.tsx` to stay in sync. 3) Consider grouping into sections if the flat list gets long. | Discovered |
| AUD-087 | Sprint 1 | Frontend | `admin/staff/page.tsx`'s filter pills use identical styling regardless of active state, unlike every other filterable admin list page which highlights the selected filter. | Low | Medium | Add the same active/inactive conditional classNames used in `admin/users/page.tsx` to the filter links in `staff/page.tsx`. | Discovered |
| AUD-088 | Sprint 1 | Frontend | `admin/page.tsx`'s dashboard stat cards hardcode raw one-off Tailwind colors per card instead of reusing the semantic amber/green/red/blue palette already used for status pills elsewhere in admin. | Low | Low | Reuse the existing semantic status-pill classes for the dashboard cards. | Discovered |
| AUD-089 | Sprint 1 | Frontend | `admin/communities/[id]/page.tsx` uses `bg-purple-100`/`text-purple-800` for the "ADMIN" role badge — purple appears nowhere else in admin's color vocabulary (amber/green/red/gray/blue), a one-off 6th color with no defined semantic meaning. | Low | Low | Reuse an existing semantic color (e.g. blue, matching the "Primary" badge in `units/[id]/page.tsx`) instead of a one-off purple. | Discovered |
| AUD-090 | Sprint 1 | Backend | `admin/facilities/actions.ts`'s `assignAmenityLeader` silently sets `requiresApproval: true` on the facility whenever a leader is assigned, with no indication in the assign-leader form — an admin unknowingly flips the facility from auto-confirm to approval-required. | Low | Medium | Add an inline note near `AssignLeaderForm` stating that assigning a leader turns on approval-required bookings. | Discovered |
| AUD-091 | Sprint 1 | Frontend | `faq-accordion.tsx`'s search filters matching sections into `filtered` but never auto-expands them — search results appear empty until the user manually clicks each matching section header. | Low | High | When the query is non-empty and matches exist, auto-open the filtered section ids (combine manual toggles with a query-driven auto-expand set). | Discovered |
| AUD-092 | Sprint 1 | Frontend | `command-palette.tsx` and `mobile-search.tsx` are dead code (zero importers) and are already broken against the current search API response shape (`{ groups }` vs the `{ users, units }` they expect). | Low | Medium | Delete both files; extend `GlobalSearchDialog`/`CasualHeader` instead if a dedicated mobile search entry point is needed. | Discovered |
| AUD-093 | Sprint 1 | Frontend | `global-search-dialog.tsx`'s `typeToFeature` maps `faq`, `pet`, `vehicle`, `user`, and `unit` results all to the same "directory" (teal) color, so 5 visually distinct result groups render with an identical icon tile/color. | Low | Medium | Add distinct `FeatureKey` entries (reusing the existing indigo `faq` color instead of overriding it to `directory`) so each group gets a unique swatch. | Discovered |
| AUD-094 | Sprint 1 | Frontend | `global-search-dialog.tsx` has no open/close transition animation — the dialog mounts/unmounts instantly with no fade, unlike other overlays in the app. | Low | Low | Wrap the backdrop and panel in the existing `animate-fade-in-up` class (or a base-ui Dialog primitive that handles enter/exit). | Discovered |
| AUD-095 | Sprint 1 | Frontend | `faq-manage-panel.tsx`'s error message uses raw `bg-red-50 text-red-800` instead of the rose-based error convention used elsewhere in the app (e.g. `global-search-dialog.tsx`, `rich-text-editor.tsx`). | Low | Low | Replace with `bg-rose-50 text-rose-700`. | Discovered |
| AUD-096 | Sprint 1 | Frontend | `file-upload.tsx` uses a raw `<img>` for preview thumbnails instead of `next/image`, and never revokes the blob URLs it creates via `URL.createObjectURL` — a memory leak across repeated selections. | Low | Low | Call `URL.revokeObjectURL` on removal/unmount, and use `next/image` with `unoptimized` for blob URLs if adopted. | Discovered |
| AUD-097 | Sprint 1 | Frontend | `faq-manage-panel.tsx`'s edit/delete icon buttons have no `aria-label`, unlike the adjacent move-up/move-down buttons which correctly set one. | Low | Medium | Add `aria-label="Edit"`/`aria-label="Delete"` (or matching microcopy keys) to the icon buttons. | Discovered |
| AUD-098 | Sprint 1 | Backend | `api/faq/route.ts` imports `auth` but never uses it in this public GET handler — dead code that misleadingly implies auth gating that doesn't exist. | Low | Low | Remove the unused `import { auth } from "@/lib/auth"` line. | Discovered |
| AUD-099 | Sprint 1 | Frontend | FAQ pages have inconsistent unauthenticated-redirect destinations: `/faq/manage` preserves `callbackUrl`, but `/faq/app` redirects to `/faq` with no callback, losing the user's intended destination after login. | Low | Low | Make `faq/app/page.tsx` redirect to `/login?callbackUrl=/faq/app`, matching `faq/manage/page.tsx`. | Discovered |
| AUD-100 | Sprint 1 | Backend | `communities/[id]/page.tsx` runs `hasPendingRequest` as a separate sequential `await` after `isLeader`/`pendingJoinRequests` instead of parallelizing independent queries with `Promise.all`, adding avoidable latency. | Low | Medium | Combine the independent queries via `Promise.all` after the initial community fetch. | Discovered |
| AUD-101 | Sprint 1 | Frontend | `committee/page.tsx`'s member grid has no entrance animation or card-level hover state, unlike `communities/page.tsx` cards which have `hover:ring-gold hover:shadow-lg`. | Low | Low | Wrap the `.map` in `StaggerChildren` and add `hover:shadow-md` to the card. | Discovered |
| AUD-102 | Sprint 1 | Frontend | `committee/page.tsx` fetches `avatarUrl` in its query but only ever renders an initial-letter circle — the field is dead in the UI, so committee members with a photo never see it here. | Low | Medium | Render `avatarUrl` when present, falling back to the initial-letter circle otherwise. | Discovered |
| AUD-103 | Sprint 1 | Frontend | Public `committee/page.tsx` and `admin/committee/page.tsx` implement two independently-written "is current designation" filters (DB-level `gt` vs client-side JS split) that can drift apart if the definition of "current" ever changes. | Low | Low | Extract a shared `isActiveDesignation()` helper (e.g. in `designation-labels.ts`) used by both. | Discovered |
| AUD-104 | Sprint 1 | Frontend | `communities/page.tsx` doesn't use `PageHeader` despite the `"communities"` feature-color entry already existing and going unused here — title/subtitle are hand-rolled `<h1>`/`<p>`. | Low | Medium | Replace the manual heading block with `<PageHeader feature="communities" title=... subtitle=... />`. | Discovered |
| AUD-105 | Sprint 1 | Frontend | `communities/page.tsx`'s card grid has no `FadeIn`/`StaggerChildren` entrance animation despite being a list of cards analogous to other resident grids that use it. | Low | Low | Wrap the `.map` output in `StaggerChildren`. | Discovered |
| AUD-106 | Sprint 1 | Frontend | `communities/page.tsx`'s empty state is a plain centered text block instead of the shared `EmptyState` component, and `microcopy.ts` has no `communities` entry to draw copy from. | Low | Low | Add a `communities` entry to the `empty` object in `microcopy.ts`, then use `EmptyState` with that copy. | Discovered |
| AUD-107 | Sprint 1 | Frontend | `communities/[id]/page.tsx` denotes the "Admin" community role with an unlabeled "★" suffix — a screen reader announces only "black star" with no context. | Low | Low | Add `<span className="sr-only">Admin</span>` (or an `aria-label`) alongside the star. | Discovered |
| AUD-108 | Sprint 1 | Backend | `hub-data.ts` coerces `primaryUnit.floor` to `0` via `?? 0` when the DB value is null, silently conflating "unknown floor" with an actual ground floor (`HubUser.primaryUnit.floor` is typed as required `number`). | Low | Medium | Change the type to `number | null`, pass the raw null through, and have consumers render "Floor —" (or omit) when null. | Discovered |
| AUD-109 | Sprint 1 | Backend | `hub-data.ts` duplicates the same 6 queries (notices/events/polls/facilities/forumThreads/faqCount) verbatim between `getGuestHubData` and `getResidentHubData` with no shared helper — any future personalization must be edited in two places. | Low | Low | Extract a shared `getPublicHubSlice()` helper called by both, with `getResidentHubData` adding only the tower filter and count queries. | Discovered |
| AUD-110 | Sprint 1 | Backend | `api/admin/committee/route.ts` has no date validation (`isNaN` check, `endDate > startDate`), contradicting the project's own convention applied to other forms (events/visitors/polls), and would surface an opaque Prisma 500 on garbage input instead of a clean 400. | Low | Medium | Add `isNaN`/end-after-start checks before creating the designation, and mirror them in `committee-form.tsx` client-side. | Discovered |
| AUD-111 | Sprint 1 | Frontend | `admin/committee/committee-form.tsx`'s hardcoded `TITLES` array duplicates (and has already drifted in ordering from) the canonical `DESIGNATION_TITLES` in `src/lib/designation-labels.ts`. | Low | Low | Derive the `<option>` list from `DESIGNATION_TITLES`/`designationTitleLabels` instead of a local array. | Discovered |
| AUD-112 | Sprint 1 | Frontend | `microcopy.ts` has no dedicated copy blocks for "communities" or "committee", unlike newer features (`staff`, `contacts`, `faq`) which each got a full block — these two pages fall back to raw hardcoded strings. | Low | Low | Add `communities` and `committee` blocks to `microcopy.ts` and update both pages to consume them. | Discovered |
| AUD-113 | Sprint 1 | Frontend | `communities/[id]/page.tsx` uses raw `.toLocaleDateString()` with no locale/format options at 3 call sites, inconsistent with `committee/page.tsx` which explicitly passes `"en-IN"` — risks an `MM/DD/YYYY` vs `DD/MM/YYYY` mismatch for this India-based audience. | Low | Low | Pass `.toLocaleDateString("en-IN", { day: "numeric", month: "short", year: "numeric" })` consistently at all 3 call sites. | Discovered |
| AUD-114 | Sprint 2 | Frontend | Three separate hardcoded nav-item arrays exist with no shared source of truth — `headerNav` (`casual-header.tsx`), `moreLinks` (`mobile-bottom-nav.tsx`), and `navItems` (`horizontal-nav-pills.tsx`) — and they already disagree on which features count as "primary" nav. | Medium | Medium | 1) Create a shared `src/lib/nav-items.ts` exporting arrays per context. 2) Import from it in all three files. 3) Visually verify no nav item is silently dropped during the refactor. | Discovered |
| AUD-115 | Sprint 2 | Frontend | The community logo image is loaded from a hardcoded external URL (`https://www.gulshandynasty.com/...`) with no fallback if the third-party site is down, and the same URL is duplicated in both `casual-header.tsx` and `login/page.tsx`. | Medium | Medium | Copy the logo asset into `public/` and reference it locally from both files, removing the duplicated external URL. | Discovered |
| AUD-116 | Sprint 2 | Frontend | `login/page.tsx`'s "Continue with Google/Apple" button in the collapsed state only calls `setShowProviders(true)`, revealing a second, nearly-identical set of provider buttons that actually call `signIn` — ~60 lines of duplicated SVG/markup for an unexplained two-click flow. | Medium | Medium | Decide if the two-step reveal is intentional; if not, delete the first inert button pair and call `signIn` directly from the always-visible buttons. | Discovered |
| AUD-117 | Sprint 2 | Frontend | In the `showProviders` branch, `login/page.tsx`'s magic-link form calls `signIn("email", ...)` directly with no `useTransition`/loading state and no "Check your email!" confirmation, unlike the first magic-link form on the same page. | Medium | Medium | Reuse the same `startSending`/`setEmailSent` handler from the first form; consider deduplicating the two forms into one shared sub-component. | Discovered |
| AUD-118 | Sprint 2 | Frontend | `leader/page.tsx` runs a separate `groupBy` query (`pendingByFacility`) just to get counts that could be derived from the already-fetched `pendingQueue` result — the DB is queried twice for overlapping data. | Medium | Low | Remove the separate `groupBy` query; derive the pending-count map by reducing over `pendingQueue`. | Discovered |
| AUD-119 | Sprint 2 | Frontend | `leader-hub.tsx` hardcodes per-item icon colors (teal for community, emerald for amenity, violet for FAQ) instead of referencing `featureColors` — `communities` is teal here but amber everywhere else in the app, a real cross-page color mismatch. | Medium | Medium | Reference `featureColors.communities`/`featureColors.facilities` etc. for each row's icon/text color instead of inline hardcoded classes. | Discovered |
| AUD-120 | Sprint 2 | Frontend | `leader-hub.tsx` uses raw hand-rolled `Link` cards (`rounded-xl border bg-card p-4 hover:bg-muted/50`) for every row instead of the shared `SoftCard` component, producing a different shadow/border treatment than other resident list pages. | Medium | Low | Wrap each row's content in `SoftCard` to match the shadow/ring treatment used elsewhere. | Discovered |
| AUD-121 | Sprint 2 | Frontend | `hub-hero.tsx`'s time-of-day greeting is computed server-side on a `force-dynamic` page, so it reflects the server's clock/timezone rather than the visiting resident's local time. | Medium | Low | Move the time-of-day computation to a small client subcomponent using `Date` on mount, or explicitly document server-timezone as acceptable for this single-timezone community. | Discovered |
| AUD-122 | Sprint 2 | Frontend | `feature-colors.ts`'s `communities` label ("Teams and Communities") overlaps confusingly with the separate `team` feature ("Meet Our Team") — both mention "team(s)" yet map to different routes/colors. | Medium | Medium | Rename the `communities` label to something unambiguous (e.g. "Communities" or "Interest groups") to avoid duplicate "team(s)" wording across two distinct features. | Discovered |
| AUD-123 | Sprint 2 | Frontend | `ContactReviewForm` and `StaffReviewForm` — two near-duplicate review systems — have drifted in their prop contracts: one has a `disabled` concept and a hard `window.location.reload()`, the other has an `onSuccess` callback but no `disabled` equivalent. | Medium | Low | 1) Decide a single shared prop contract (both take `onSuccess`, both support `disabled`). 2) Extract a shared `ReviewForm` parameterized by API path and copy. 3) Update both call sites to the unified API. | Discovered |
| AUD-124 | Sprint 2 | Frontend | `contacts-list.tsx`'s `categoryColors` hardcodes 16 raw pastel classes independent of the design-token-driven pattern used elsewhere, with several categories accidentally sharing identical colors ("Internal" and "Staples" both amber, "Personal Care" and "Health" both rose). | Medium | Low | 1) Extract `categoryColors`/`categoryIcons` into a `src/lib/contact-category-style.ts` mirroring `staff-role-style.ts`'s shape. 2) De-duplicate the accidental color collisions. 3) Reuse the module for both the category header and filter pills. | Discovered |
| AUD-125 | Sprint 2 | Frontend | `contacts/[id]/page.tsx` and `staff/[id]/page.tsx` "hero" cards diverge structurally: contacts uses a plain stacked layout with no avatar, while staff uses a `flex sm:flex-row` layout with a 16×16 avatar/initials tile — the two conceptually similar detail pages look structurally unrelated. | Medium | Medium | 1) Decide whether `ImportantContact` should get avatar/initials treatment like staff. 2) Extract a shared `DetailHeroCard` component used by both. 3) Keep `SoftCard` accent colors as the only visual differentiator. | Discovered |
| AUD-126 | Sprint 2 | Frontend | Contact cards are plain `<Link>` elements with no left accent stripe, while the near-identical staff cards are `<article>` elements with a colored accent stripe, icon tile, and richer card chrome — contacts look visually flatter despite being sibling registries. | Medium | Medium | Add a left accent stripe and icon tile to contact cards using the category color module (see AUD-124), and match the `shadow-sm ring-1 ring-foreground/5 hover:shadow-md` chrome from `help-tab-content.tsx`. | Discovered |
| AUD-127 | Sprint 2 | Frontend | `staff/page.tsx` has no `Suspense`/skeleton fallback around `HelpTabContent`, unlike other data-heavy pages, so the page renders an empty shell before the client-side fetch resolves. | Medium | Medium | Wrap the list rendering in `<Suspense fallback={<ListSkeleton />}>` once staff data fetching moves server-side (see AUD-131). | Discovered |
| AUD-128 | Sprint 2 | Frontend | Three separate hardcoded tower-color maps exist across the codebase (`units/[unitNumber]/page.tsx`'s `towerColors`, `directory-tower-filter.tsx`'s `towerFilterStyles`, and `directory-layout.ts`'s `towerBalloonStyles`) with slightly different exact shades for the same gold/teal/rose palette. | Medium | Low | 1) Move `towerColors`-style badge classes into `directory-layout.ts` as a new exported map. 2) Replace the inline map in `units/[unitNumber]/page.tsx` with an import from it. 3) Consider having `directory-tower-filter.tsx` reuse the same base tokens. | Discovered |
| AUD-129 | Sprint 2 | Frontend | `directory-tower-filter.tsx`'s `useEffect` silently redirects any compact-viewport user with no tower selected to `?tower=A`, regardless of who they actually are — a resident lands on Tower A by default instead of their own tower. | Medium | Low | Default compact view to the user's own tower (derivable from their unit membership) rather than hardcoding Tower A. | Discovered |
| AUD-130 | Sprint 2 | Backend | The visitor-pass OTP (`crypto.randomInt(100000,999999)`) has no DB-level uniqueness constraint; if two active passes ever collide on OTP, a guard could validate the wrong visitor's pass with no way to detect it. | Medium | Medium | 1) Add a unique/partial-unique index on `otp` where `status = "ACTIVE"`. 2) Retry OTP generation on creation if a collision exists among active passes. | Discovered |
| AUD-131 | Sprint 2 | Backend | `visitors/validate/route.ts` re-fetches `resolveStaffDestinationUnits()` twice and runs a per-destination `findMany` in a loop (N+1) before sending notifications — directly adding latency to the guard-facing "Validating…" spinner. | Medium | Medium | 1) Reuse the `destinations` array already fetched earlier instead of re-fetching. 2) Replace the per-destination loop with a single `findMany({ where: { unitId: { in: unitIds }, ... } })`. 3) Consider firing notifications without blocking the validation response. | Discovered |
| AUD-132 | Sprint 2 | Frontend | `booking-grid.tsx` uses native `window.confirm()` for booking confirmation, breaking the app's animated, casual design language with a jarring browser dialog. | Medium | Medium | Replace `confirm()` with a controlled confirmation modal/sheet matching the app's visual style, showing slot details before proceeding. | Discovered |
| AUD-133 | Sprint 2 | Frontend | `booking-grid.tsx`'s `getSlotStatus` is an unmemoized function recomputing Date objects and filtering `existingBookings`/`blackouts` from scratch for every cell on every render (O(days × slots × bookings)). | Medium | Medium | Precompute a lookup map of slot statuses once per `(weekStart, existingBookings, blackouts)` change via `useMemo`, keyed by day+slot index. | Discovered |
| AUD-134 | Sprint 2 | Frontend | `booking-grid.tsx`'s `cellColors` (desktop) and `mobileSlotClass` (mobile) both hardcode the same 4-state slot-availability colors separately with slightly different opacity/border treatments between the two views. | Medium | Low | Define a single `SLOT_STATUS_STYLES` map with variants for both contexts and reference it from both `cellColors` and `mobileSlotClass`. | Discovered |
| AUD-135 | Sprint 2 | Frontend | Every admin list page (`users`, `moves`, `forums`, `tickets`, `notices`, `dues`, `dues/report`) defines its own inline status/priority color map rather than sharing one, so a future color change must be replicated across 6+ files. | Medium | Medium | 1) Create `src/lib/admin-status-colors.ts` covering every status enum used across admin. 2) Replace each page's local map with it. 3) Wrap the badge markup in one shared `AdminStatusBadge` component. | Discovered |
| AUD-136 | Sprint 2 | Frontend | `admin/units/page.tsx` is the only admin list page lacking the `md:hidden`/`hidden md:block` mobile-card + desktop-table split — it renders a bare `<table>` unconditionally, which AGENTS.md explicitly flags as a pitfall. | Medium | Medium | Add a `md:hidden` card list view (mirroring `admin/moves/page.tsx` or `vehicles/page.tsx`) and wrap the existing table in `hidden md:block`. | Discovered |
| AUD-137 | Sprint 2 | Frontend | `admin/staff/page.tsx`'s table has no `overflow-x-auto` wrapper and no mobile-card view — it overflows the page horizontally on narrow viewports, unlike every other admin table. | Medium | Medium | 1) Wrap the table in `<div className="overflow-x-auto">`. 2) Add a `md:hidden` card list view and wrap the table in `hidden md:block`. | Discovered |
| AUD-138 | Sprint 2 | Frontend | `admin/staff/[id]/page.tsx` renders association/review/pass status and "needs review" as raw unstyled text — a third distinct visual treatment of the same concept (vs. the emoji on the list page and pill badges elsewhere in admin). | Medium | Medium | Replace raw status text with the shared status badge component (AUD-135), and standardize "needs review" to a pill treatment consistent with `staff/page.tsx`. | Discovered |
| AUD-139 | Sprint 2 | Backend | `api/contacts/route.ts`'s POST handler never validates `category`/`typeOfService` against a known set — any free-text string is accepted, so typos fragment categories (e.g. "Plumber" vs "plumber") into separate filter groups. | Medium | Medium | 1) Define a canonical category list (alongside `NON_REVIEWABLE_CATEGORY` in `contact-reviews.ts`). 2) Validate `category` against it in the POST handler, returning 400 on unknown values, and drive the create-contact form's dropdown from the same list. | Discovered |
| AUD-140 | Sprint 2 | Backend | `api/contacts/[id]/reviews/route.ts`'s rate limiter is an in-memory `Map` inside the route module — it resets on restart and doesn't work across multiple serverless instances, so the "10 reviews/min" cap is not actually enforced globally. | Medium | Low | Move rate limiting to a shared store if it needs to hold under multi-instance deployment; otherwise document it as best-effort per-instance only. | Discovered |
| AUD-141 | Sprint 2 | Frontend | An admin-role gate check (`!["SUPER_ADMIN","ADMIN"].includes(...)`) is duplicated inline in 4+ files instead of reusing the shared `isAdmin()` helper from `@/lib/rbac` that `staff/page.tsx` already uses correctly. | Medium | Low | Standardize on the shared `isAdmin()` helper everywhere an admin-role gate is needed, removing the duplicated local implementations. | Discovered |
| AUD-142 | Sprint 2 | Frontend | `global-search-dialog.tsx`'s search API results are inconsistent in which fields are populated across categories (`user` duplicates tower info between `subtitle`/`meta`; `staff` never sets `meta` at all), producing ragged-looking result rows. | Medium | Low | Normalize which fields each `SearchResultType` populates (e.g. `meta` always for a short tag, `subtitle` always for descriptive text) across the ~14 category builders in `api/search/route.ts`. | Discovered |
| AUD-143 | Sprint 2 | Frontend | `global-search-dialog.tsx` has no request cancellation/race-guard — if an earlier, slower query's response arrives after a later, faster one, the stale response can overwrite the fresher results. | Medium | Low | Add an `AbortController` per request (aborting on new keystroke) or a request-id ref, ignoring responses that don't match the latest issued request. | Discovered |
| AUD-144 | Sprint 2 | Frontend | `rich-text-editor.tsx`'s character counter never visually warns when the value exceeds `maxLength`, and there's no client-side enforcement — users only discover the 10,000-char cap when the server rejects the save. | Medium | Medium | 1) Turn the counter rose/red when over the limit. 2) Optionally add a soft client-side guard (e.g. a CharacterCount extension) for immediate feedback. | Discovered |
| AUD-145 | Sprint 2 | Frontend | `file-upload.tsx` silently truncates the selection to `maxFiles` via `.slice()` with no feedback when the user selects more than the limit. | Medium | Medium | Surface an inline message ("Only the first N photos were kept") when `files.length > maxFiles`. | Discovered |
| AUD-146 | Sprint 2 | Frontend | `faq-manage-panel.tsx`'s `moveItem`/`moveSection` reorder actions call a full `router.refresh()` after every single up/down click with no optimistic update — rapid clicking produces a burst of refreshes and the button appears to do nothing until each completes. | Medium | Low | Apply the array swap optimistically to local state immediately, then reconcile with the server response in the background. | Discovered |
| AUD-147 | Sprint 2 | Frontend | `faq-manage-panel.tsx`'s section/item delete uses native `window.confirm()`, jarring and unstyled compared to the app's design system. | Medium | Low | Replace with the app's existing dialog/alert-dialog primitive for an on-brand confirmation modal. | Discovered |
| AUD-148 | Sprint 2 | Frontend | The `/faq` page's redirect for logged-in users happens client-side after hydration (reading `window.location.hash` before calling `router.replace`) instead of server-side, causing a visible flash of the public FAQ shell before redirecting (and no redirect at all with JS disabled). | Medium | Medium | Perform the redirect server-side (`redirect("/faq/app")`) for the common no-hash case in `faq/page.tsx`, keeping the client component only for the hash-preservation edge case. | Discovered |
| AUD-149 | Sprint 2 | Frontend | `communities/[id]/page.tsx` renders 4 mutually-exclusive membership-status badges (join button, tower-only notice, pending, member) and `communities/page.tsx` renders its own "Member"/"View" badges — all via hardcoded, slightly different (padding/size) inline colors instead of a shared badge component, so the identical "Member" state looks different on the list vs. detail page. | Medium | Medium | Add "member"/"pending"/"admin-role"/"view" badge variants to `FriendlyBadge` (or a small shared `StatusPill`) and replace all the hardcoded spans on both pages with it. | Discovered |
| AUD-150 | Sprint 2 | Frontend | Neither `communities/page.tsx` nor `communities/[id]/page.tsx` has a `Suspense` boundary or loading skeleton, despite `src/components/shared/skeletons.tsx` existing for exactly this purpose — navigation blocks on all sequential DB calls with no visible loading state. | Medium | Medium | Split each page into a thin shell plus an inner async component wrapped in `<Suspense fallback={<...Skeleton />}>`, adding a skeleton variant if missing. | Discovered |
| AUD-151 | Sprint 2 | Frontend | `casual-header.tsx`'s search trigger reuses the same modal-sized `GlobalSearchDialog` panel at every breakpoint instead of a full-screen/bottom-sheet layout on mobile, unlike the abandoned `MobileSearch` component which had a proper touch-friendly layout. | Medium | Low | On small screens, render the dialog panel full-width/full-height (`inset-0`, no `max-w-lg` cap) instead of a shrunk desktop-style modal. | Discovered |
| AUD-152 | Sprint 3 | Frontend | `HelpTabContent` is a full client component that fetches all staff data client-side via `useEffect` + `fetch("/api/staff")` after mount, showing a loading spinner on every page load, while `contacts/page.tsx` fetches server-side and passes props with no spinner or waterfall — the single biggest perf/consistency gap between these two sibling list pages. | High | High | 1) Move the staff-list DB query into `src/app/staff/page.tsx` as a server-side fetch, mirroring `contacts/page.tsx`'s `db.importantContact.findMany` call. 2) Pass the resulting list as a prop into `HelpTabContent`, keeping it client-side only for interactive filtering/add/remove. 3) Keep a `router.refresh()`-based reload path for mutations instead of the current fetch-on-mount pattern. | Discovered |
| AUD-153 | Sprint 3 | Frontend | `units/[unitNumber]/page.tsx` and `users/[userId]/page.tsx` use raw shadcn `Card`/`CardHeader`/`Badge` enterprise-style markup instead of the `SoftCard`/`FriendlyBadge`/`PageHeader` conventions used throughout the rest of the audited resident UI (contacts, staff). | High | Medium | 1) Replace the raw `<h1>` headers with `<PageHeader feature="directory" .../>` on both pages. 2) Swap `Card`/`CardContent` wrappers for `SoftCard`, and raw `<Badge>{role.replace(...)}</Badge>` for `FriendlyBadge` wherever role/status enums are rendered. 3) Consider scoping the badges first (lower risk) before migrating card chrome. | Discovered |
| AUD-154 | Sprint 3 | Frontend | Several admin pages (`committee`, `communities`, `dues`, `export`, `audit`, `notices/new`, `forums`, `tickets`) wrap their content in `DashboardLayout` (the resident casual shell) even though `admin/layout.tsx` already wraps every `/admin/*` route in its own dark-sidebar enterprise chrome — producing a double-shell / nested-navigation bug on those pages. | High | High | 1) Load `/admin/committee` and confirm whether the casual header/bottom-nav renders nested inside the dark admin sidebar. 2) If confirmed, remove the `DashboardLayout` wrapper from all affected admin pages. 3) Standardize every admin page on the `Breadcrumb`-only convention already used by `facilities`, `vehicles`, `moves`, `staff`, and `units`. | Discovered |

---

## Notes for the executor

- Every item was tied to a real file read during this audit; none are speculative. Re-verify against current code before fixing per the standard staleness caveat — some may already be resolved by concurrent work.
- **AUD-021, AUD-022, AUD-023, AUD-025** are outright runtime bugs (not just polish) and should be prioritized regardless of sprint bucket: a broken hide/unhide review action, a broken admin approve/reject UI, silent data loss on file removal, and a garbled page title respectively.
- **AUD-152** (staff data-fetch waterfall) and **AUD-154** (admin double-shell) are the two highest-leverage structural fixes — both affect an entire feature area rather than one component.
- Several findings recommend the same underlying fix (e.g. adopting `FriendlyBadge` everywhere, replacing `window.location.reload()` with `router.refresh()`, adding `Suspense`/skeletons) — consider batching those by pattern rather than fixing file-by-file.
