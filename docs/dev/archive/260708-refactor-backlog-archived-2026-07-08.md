# Archived — 260708-refactor-backlog.md
_Archived: 2026-07-08 · Source: `docs/dev/tracking/260708-refactor-backlog.md` · Count: 60 completed/skipped items (updated 2026-07-08)_

---

## Items

| ID | Sprint | Area | Description | Complexity | Impact | Pointers to Solve | Status |
|----|--------|------|-------------|------------|--------|-------------------|--------|
| AUD-192 | Sprint 1 | Backend | `hub-data.ts` fetches active polls/events with no sub-community scope filter, while `api/polls/route.ts` and `api/events/route.ts` apply `buildSubCommunityContentFilter()`. Sub-community polls/events can leak into the hub feed for non-members. | Medium | High | Apply `buildSubCommunityContentFilter(userId)` in `getResidentHubData()` poll/event queries; keep guest path global-only. | Done |
| AUD-193 | Sprint 1 | Backend | Notice visibility is inconsistent: `hub-data.ts` and `notices/page.tsx` filter by tower block only; `api/notices/route.ts` and `api/search/route.ts` use `buildNoticeVisibilityFilter()` (sub-community + tower). Hub feed and notices page can show wrong notices or miss community-scoped ones. | Medium | High | Replace inline tower filters in `hub-data.ts` and `notices/page.tsx` with `buildNoticeVisibilityFilter()`. | Done |
| AUD-198 | Sprint 1 | Backend | `api/media/route.ts` only bypasses auth for `uploads/hub-hero/` keys. FAQ rich-text images live under `uploads/faq/` and require a session — broken for guests on public `/faq`. | Low | High | Add `uploads/faq/` to the public prefix list, or serve FAQ images through a dedicated public proxy. | Done |
| AUD-211 | Sprint 1 | Backend | `api/files/delete/route.ts` deletes any `FileEntry` by ID for any authenticated user — no uploader, admin, or ownership check. | Low | High | Gate on `uploadedById === session.user.id` or `isAdmin()`; optionally restrict by `subCommunityId` membership. | Done |
| AUD-212 | Sprint 1 | Backend | `hub-data.ts` primary-unit lookup uses `where: { endDate: null }` while `rbac.ts` and the rest of the codebase use `OR: [{ endDate: null }, { endDate: { gt: now } }]`. Users with future end dates get no/wrong primary unit on hub. | Low | Medium | Align membership filters with the shared active-membership pattern from `rbac.ts`. | Done |
| AUD-187 | Sprint 1 | Frontend | `notification-list.tsx` optimistically marks notifications read without checking `res.ok` — failed API calls show items as read incorrectly. | Low | Medium | Check `res.ok`; revert optimistic update on failure; show inline error. | Done |
| AUD-155 | Sprint 2 | Frontend | Two unrelated components named `FileUpload`: photo picker in `shared/file-upload.tsx` vs presigned document upload in `files/file-vault.tsx` (imported as `FileUpload` in `files/page.tsx:5`). | Medium | Medium | Rename vault export to `DocumentUpload`; reserve `FileUpload` for shared photo UX. | Done |
| AUD-156 | Sprint 2 | Frontend | `formatSize()` + `getFileIcon()` duplicated in `files/page.tsx:27-39` and `file-vault.tsx:106-118`; file row markup duplicated between page and `FileList`. | Low | Low | Extract `formatBytes()` / `mimeTypeIcon()` to `lib/format-files.ts`; extract shared `FileListRow`. | Done |
| AUD-157 | Sprint 2 | Frontend | Hand-rolled search input with absolute `Search` icon in `contacts-list.tsx:127`, `staff-search.tsx:67`, `faq-accordion.tsx:74`. | Low | Low | Extract `SearchInput` in `shared/search-input.tsx`. | Done |
| AUD-158 | Sprint 2 | Frontend | Filter-pill scroll row (`overflow-x-auto flex-nowrap snap-x snap-mandatory scrollbar-hide`) duplicated in 6+ files: `contacts-list.tsx`, `help-tab-content.tsx`, `visitors/page.tsx`, `booking-grid.tsx`, `directory-tower-filter.tsx`. | Low | Low | Extract `FilterPillRow` wrapper in `shared/filter-pill-row.tsx`. | Done |
| AUD-167 | Sprint 2 | Frontend | ~22 files duplicate inline error banner markup (`rounded-lg bg-red-50 p-3 text-sm text-red-800`); `hub-hero-manage-panel.tsx` uses a third variant (`destructive` tokens). | Medium | Low | Add shared `FormError` / `InlineAlert` with rose-based variant; replace all inline copies. | Done |
| AUD-168 | Sprint 2 | Frontend | Identical `apiCall()` + `refresh()` (`useTransition` + `router.refresh()`) in `hub-hero-manage-panel.tsx:43-48` and `faq-manage-panel.tsx:24-30`. | Medium | Low | Extract `useJsonMutation()` or `createApiClient()` hook in `lib/client-api.ts`. | Done |
| AUD-169 | Sprint 2 | Frontend | Move up/down button clusters duplicated in `hub-hero-manage-panel.tsx:307-326` and `faq-manage-panel.tsx` (4× in FAQ). | Low | Low | Extract `ReorderButtons` in `shared/reorder-buttons.tsx`. | Done |
| AUD-174 | Sprint 2 | Frontend | Hub hero delete uses shadcn `Dialog` (`hub-hero-manage-panel.tsx:344-361`); FAQ delete uses `window.confirm` (`faq-manage-panel.tsx:156/345`) — same editor role, inconsistent UX. | Low | Medium | Extract shared `ConfirmDialog`; migrate FAQ delete to match hub-hero pattern. | Done |
| AUD-175 | Sprint 2 | Backend | Hub hero reorder swaps two items via parallel PATCH calls; FAQ reorder uses batch `{ reorder: true, ids }`. Hub hero API has no equivalent batch endpoint. | Medium | Low | Add `{ reorder: true, ids }` to `api/hub/hero-slides/`; align manage panels on one reorder contract. | Done |
| AUD-176 | Sprint 2 | Backend | `hub-hero-auth.ts` is a one-line alias of `canManageFaq()` — misleading name for shared FAQ/hub/files-upload permission. | Low | Low | Rename to `canManageCommunityContent()` in `faq-auth.ts`; update all call sites. | Done |
| AUD-179 | Sprint 2 | Frontend | Green/red Approve/Reject button pairs copy-pasted in `community-leader-panel.tsx`, `join-request-row.tsx`, `my-bookings.tsx`, `pending-bookings.tsx`. | Medium | Low | Extract `ApproveRejectButtons` with `pending`, `onApprove`, `onReject`, size variants. | Done |
| AUD-183 | Sprint 2 | Frontend | Booking rejection uses `window.prompt()` in `my-bookings.tsx:105` and `pending-bookings.tsx:100`; success paths diverge (reload vs `router.refresh()`+refetch). | Medium | Medium | Extract shared `RejectBookingDialog` with reason field; unify refresh strategy. | Done |
| AUD-185 | Sprint 2 | Frontend | Purple pills (`bg-purple-100 text-purple-800`) used for unrelated semantics in 6+ files: Recurring, Resolution, community admin, amenity leaders. | Low | Low | Extend `FriendlyBadge` or add `SemanticBadge` variants for `RECURRING`, `RESOLUTION`, `LEADER`, `ADMIN_ROLE`. | Done |
| AUD-194 | Sprint 2 | Backend | 10+ API routes inline `["SUPER_ADMIN","ADMIN"].includes(...)` instead of `isAdmin()` from `rbac.ts`. Examples: `admin/committee`, `admin/users`, `admin/memberships`, `dues/generate`, `dues/mark-paid`. | Low | Medium | Add `requireAdminApi()` in `lib/rbac.ts` returning `{ userId } \| NextResponse`; replace all inline checks. | Done |
| AUD-181 | Sprint 3 | Frontend | AUD-053 lists 6 reload sites; **16 additional files** still use `window.location.reload()`: `my-bookings.tsx`, `community-leader-panel.tsx`, `contacts-list.tsx`, `rsvp-buttons.tsx`, `vote-form.tsx`, `comment-form.tsx`, `join-button.tsx`, `booking-grid.tsx`, etc. | Low | Medium | Batch-replace with `router.refresh()` + optimistic/local state updates. | Done |
| AUD-182 | Sprint 3 | Frontend | Native `confirm()` in **13 files** beyond AUD-147/AUD-132: `faq-manage-panel.tsx`, `help-tab-content.tsx`, `unit-vehicles-section.tsx`, `unit-pets-section.tsx`, `generate-dues-form.tsx`, `admin-user-actions.tsx`, `files/delete-action.tsx`, `archive-button.tsx`, etc. | Medium | Medium | Shared `ConfirmDialog` / AlertDialog wrapper; batch migration after AUD-174. | Done |
| AUD-188 | Sprint 3 | Frontend | `contacts-list.tsx:109-121` reloads on success via `window.location.reload()`, silently ignores `!res.ok`. | Low | Medium | Add error state; use `router.refresh()`. | Done |
| AUD-189 | Sprint 3 | Frontend | `success-animation.tsx` has **zero importers** — dead shared component despite being recommended in AUD-073. | Low | Medium | Wire into booking/RSVP/vote success flows, or delete. | Done |
| AUD-190 | Sprint 3 | Frontend | `events/[id]/rsvp-buttons.tsx:21-28` POSTs to `/api/events/rsvp` with no `res.ok` check before `window.location.reload()`. | Low | Medium | Check response; show inline error; use `router.refresh()`. | Done |
| AUD-197 | Sprint 3 | Backend | Image upload allowlist and 5 MB cap triplicated: `FAQ_IMAGE_TYPES`/`FAQ_MAX_SIZE` in `files/upload/route.ts`, `IMAGE_TYPES`/`MAX_IMAGE_SIZE` in `rich-text-upload.ts`, duplicated validation for `faq` vs `hub-hero` namespaces. | Low | Medium | Create `lib/upload-config.ts` with shared constants + `validateImageUpload()`. | Done |
| AUD-199 | Sprint 3 | Backend | `files/upload/route.ts` uses sequential `if (namespace === "faq")` / `if (namespace === "hub-hero")` blocks with copy-pasted validation. | Medium | Medium | Replace with `UPLOAD_NAMESPACES` config map: `{ authGuard, allowedTypes, maxSize, keyPrefix }`. | Done |
| AUD-200 | Sprint 3 | Backend | Three separate in-memory rate-limit `Map`s with identical logic: `contacts/[id]/reviews/route.ts`, `staff/[id]/reviews/route.ts`, `staff/search/route.ts`. FAQ routes correctly use `lib/rate-limit.ts`. (Extends AUD-140.) | Low | Medium | Add `checkUserRateLimit()` wrapper; delete local Maps; use `rateLimitResponse()` for 429s. | Done |
| AUD-214 | Sprint 3 | Backend | API error strings hardcoded and inconsistent: `"Unauthorized"`, `"Forbidden"`, `"Approval required"`, `"Too many requests"` (no `Retry-After`) vs FAQ's `rateLimitResponse()` message. | Low | Low | Add `lib/api-errors.ts`; use `rateLimitResponse()` everywhere for 429s. | Done |
| AUD-186 | Sprint 4 | Frontend | `pending-bookings.tsx:50-52` always refetches on mount even when `facilities/[id]/page.tsx:138-143` passes `initialBookings`. | Low | Medium | Skip initial fetch when initial data provided; refetch only after mutations. | Done — `pending-bookings.tsx:53-55` skips fetch when `initialBookings.length > 0`. |
| AUD-159 | Sprint 4 | Frontend | `packages/page.tsx` — hand-rolled h1, inline empty state, raw status pills; no `PageHeader` / `EmptyState` / `FriendlyBadge` / `SoftCard`. | Low | Medium | Add `packages` to `feature-colors.ts` + `microcopy.ts`; mirror `dues/page.tsx` conventions. | ✅ Completed — Added `packages` FeatureKey to `feature-colors.ts` + empty state copy to `microcopy.ts`; rewrote `packages/page.tsx` with `PageHeader`, `EmptyState`, `FriendlyBadge`. |
| AUD-160 | Sprint 4 | Frontend | `notifications/page.tsx:33-36` hand-rolled h1 despite `feature-colors.notifications`; `notification-list.tsx:92-95` inline empty text. | Low | Medium | Adopt `PageHeader feature="notifications"` + `EmptyState`. | ✅ Completed — Replaced hand-rolled h1 with `PageHeader feature="notifications"`; added `notifications` empty state copy to `microcopy.ts`. |
| AUD-161 | Sprint 4 | Frontend | `files/page.tsx:44-72` — no `PageHeader` or `EmptyState`. | Low | Medium | Add `files` feature key + microcopy; use shared header/empty components. | ✅ Completed — Added `files` FeatureKey to `feature-colors.ts` + empty state copy to `microcopy.ts`; rewrote `files/page.tsx` with `PageHeader` and `EmptyState`. |
| AUD-162 | Sprint 4 | Frontend | `profile/page.tsx:63` uses `feature="directory"` for "My Profile" — wrong icon/color. | Low | Low | Add `"profile"` to `FeatureKey`. | ✅ Completed — Added `profile` FeatureKey (zinc, User icon) to `feature-colors.ts`; changed `feature="directory"` to `feature="profile"`. |
| AUD-163 | Sprint 4 | Frontend | `tickets/[id]/page.tsx:14-62` local `statusStyles` + raw enum text. | Low | Medium | Use `FriendlyBadge` on detail page. | ✅ Completed — Removed local `statusStyles` map; replaced inline status badge with `<FriendlyBadge>`. |
| AUD-164 | Sprint 4 | Frontend | `events/[id]/page.tsx` — no `PageHeader`/`Breadcrumb`; `← Events` link. | Medium | Low | Match forums detail pattern. | ✅ Completed — Replaced `← Events` link with `<Breadcrumb>`. |
| AUD-165 | Sprint 4 | Frontend | `polls/[id]/page.tsx` — no `PageHeader`/`Breadcrumb`. | Low | Low | Add header + breadcrumb. | ✅ Completed — Added `<Breadcrumb>`. |
| AUD-166 | Sprint 4 | Frontend | `facilities/[id]/page.tsx` — no `PageHeader`; `← Facilities`. | Low | Medium | Add `Breadcrumb`. | ✅ Completed — Replaced `← Facilities` with `<Breadcrumb>`. |
| AUD-177 | Sprint 4 | Frontend | `hub/hero/manage/page.tsx` hardcodes title/subtitle/button text. | Low | Low | Add `hubHero` block to `microcopy.ts`. | ✅ Completed — Added `hubHero` export to `microcopy.ts`; updated page to source from it. |
| AUD-178 | Sprint 4 | Frontend | `community-leader-panel.tsx` `JoinRequestActions` duplicates `admin/communities/join-request-row.tsx`. | Medium | Medium | Extract shared component. | Skipped — Components have different prop shapes, refresh mechanisms, error handling; too different to merge cleanly. |
| AUD-180 | Sprint 4 | Frontend | `contacts-list.tsx:66-77` `RatingBadge` is a third star+count chip. | Low | Low | Extend `StarRatingDisplay` with `showValue`. | ✅ Completed — Replaced local `RatingBadge` with `StarRatingDisplay`. |
| AUD-184 | Sprint 4 | Frontend | Detail back-nav inconsistent across pages. | Low | Low | Standardize on `Breadcrumb`. | ✅ Completed — Added `Breadcrumb` to events/[id], polls/[id], facilities/[id]. |
| AUD-191 | Sprint 4 | Frontend | `gate/page.tsx` — hand-rolled h1, raw cards. | Low | Medium | Add `gate` feature key. | ✅ Completed — Added `gate` FeatureKey to `feature-colors.ts`; replaced h1 with `PageHeader`. |
| AUD-205 | Sprint 4 | Backend | Hero slides fetched separately in `page.tsx` despite being hub-page-only. | Low | Low | Add to `getHubData()`. | Skipped — `Promise.all` already parallelizes; no perf gain from consolidation. |
| AUD-207 | Sprint 4 | Microcopy | `NAVIGATION_SHORTCUTS` hardcodes labels that duplicate `microcopy.ts`. | Low | Medium | Build shortcuts from `microcopy.ts`. | ✅ Completed — Imported `nav` from `microcopy.ts`; replaced hardcoded labels. |
| AUD-170 | Sprint 5 | Frontend | `src/components/landing/` folder (8 files) has zero external importers. | Medium | Medium | Delete the folder. | ✅ Completed — Deleted `src/components/landing/` (8 files). |
| AUD-171 | Sprint 5 | Frontend | `landing/hero-carousel.tsx` reimplements Carousel+Autoplay. | Medium | Medium | Delete with AUD-170. | ✅ Completed — Deleted with AUD-170. |
| AUD-172 | Sprint 5 | Frontend | Image URLs catalogued separately across hub + landing files. | Medium | Medium | Single catalog in `hub-images.ts`. | ✅ Completed — Landing folder deleted; URLs consolidated in `hub-images.ts`. |
| AUD-173 | Sprint 5 | Frontend | `hub-hero-carousel.tsx` is a 20-line DTO→`ImageCarousel` mapper. | Low | Low | Inline mapping in `hub-hero.tsx`. | ✅ Completed — Inlined mapping into `hub-hero.tsx`; deleted `hub-hero-carousel.tsx`. |
| AUD-195 | Sprint 5 | Backend | Identical `guardEditor()` copy-pasted in two FAQ routes. | Low | Low | Extract shared `guardFaqEditorRoute()`. | ✅ Completed — Extracted to `lib/faq-auth.ts`; replaced in both routes. |
| AUD-196 | Sprint 5 | Backend | `api/faq/manage/route.ts` GET skips FAQ write rate limit. | Low | Low | Reuse shared guard. | Skipped — manage route is GET-only; rate limiting belongs on write endpoints only. |
| AUD-201 | Sprint 5 | Backend | `staff/[id]/reviews/route.ts` GET inlines paginated review fetch. | Low | Low | Add `listStaffReviews()` to `lib/staff.ts`. | ✅ Completed — Added `listStaffReviews()` to `lib/staff.ts`; updated route. |
| AUD-202 | Sprint 5 | Backend | Contact and staff review aggregates are structurally identical. | Medium | Low | Extract generic helpers. | ✅ Completed — Created `src/lib/review-aggregates.ts`; both modules delegate to it. |
| AUD-203 | Sprint 5 | Backend | Mixed audit patterns: `logAction()` vs `db.auditLog.create()`. | Low | Low | Standardize on `logAction()`. | ✅ Completed — Replaced all 19 remaining `db.auditLog.create()` calls across 9 files. |
| AUD-204 | Sprint 5 | Backend | Unread notification count queried three ways. | Low | Low | Route through `getUnreadCount()`. | ✅ Completed — Both `hub-data.ts` and `notifications/page.tsx` now use `getUnreadCount()`. |
| AUD-206 | Sprint 5 | Types | DTO location inconsistent across files. | Medium | Medium | Move shared DTOs to `types/`. | Skipped — DTOs co-located with domain logic; moving would add import churn with no benefit. |
| AUD-208 | Sprint 5 | Backend | `forums/rbac.ts` defines local `UserContext`; redundant casts in route. | Low | Low | Add `toForumUserContext()`. | ✅ Completed — Exported `UserContext`, added `toForumUserContext()`, removed 2 inline casts. |
| AUD-209 | Sprint 5 | Backend | `canManageFaq()` semantics differ from `isAdmin()`. | Low | Medium | Document or align. | ✅ Completed — Added JSDoc documenting `isActive`/`approvalStatus` requirement. |
| AUD-210 | Sprint 5 | Backend | Five cron routes duplicate identical auth+try/catch wrappers. | Low | Low | Extract `withCronAuth()`. | ✅ Completed — Added `withCronAuth()` to `lib/cron-auth.ts`; refactored all 5 routes. |
| AUD-213 | Sprint 5 | Backend | `requireApprovedResident()` name implies boolean but returns object. | Low | Low | Rename to `getApprovedResident()`. | ✅ Completed — Renamed across 7 files. |
